import { StyleSheet } from "react-native";

const genStyles = StyleSheet.create({
    flexRow: {
        flex: 1,
        flexDirection: 'row',
        alignItems: 'center',
    },
    flexCol: {
        flex: 1,
        alignItems: 'center',
    }
})

export default genStyles;